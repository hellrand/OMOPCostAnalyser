% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matching_functions.R
\name{match_cohorts}
\alias{match_cohorts}
\title{Match Cohorts by Age and Index Date}
\usage{
match_cohorts(
  cdm,
  target_cohort_id,
  cohort_table_name = "temp_cohort_match",
  match_sex = TRUE,
  match_year_of_birth = TRUE,
  ratio = 1,
  soft_validation = FALSE,
  matched_name = "matched_cohort"
)
}
\arguments{
\item{cdm}{CDM reference object (should have temp cohort table from prepare_cohorts_for_matching)}

\item{target_cohort_id}{Target cohort definition ID}

\item{cohort_table_name}{Name of the cohort table to use (default: "temp_cohort_match")}

\item{match_sex}{Logical, whether to match on sex (default: TRUE)}

\item{match_year_of_birth}{Logical, whether to match on year of birth (default: TRUE)}

\item{ratio}{Number of allowed matches per individual in the target cohort (default 1:1)}

\item{soft_validation}{Logical, whether to perform a soft validation of consistency. If set to FALSE four additional checks will be performed: 1) a check that cohort end date is not before cohort start date, 2) a check that there are no missing values in required columns, 3) a check that cohort duration is all within observation period, and 4) that there are no overlapping cohort entries (default: FALSE)}

\item{matched_name}{Name for the matched cohort table (default: "matched_cohort")}
}
\value{
CDM object with matched cohorts added
}
\description{
Matches a target cohort to a control cohort based on demographics
}
\examples{
\dontrun{
# First prepare cohorts
cdm <- prepare_cohorts_for_matching(cdm, "user_write_schema",
                                     target_cohort_id = 18,
                                     control_cohort_id = 19)

# Then match them
cdm <- match_cohorts(cdm, 18)

}
}
